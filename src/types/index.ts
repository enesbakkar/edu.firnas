// Re-export from main types file for compatibility
export type {
  Language,
  AppTab,
  BlockCategory,
  BlockParam,
  BlockDef,
  ParamValue,
  BlockInstance,
  TelemetryData,
  BlockAction,
} from '../types';
